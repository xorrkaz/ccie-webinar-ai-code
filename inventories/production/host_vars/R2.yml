---
# R2 Production BGP Configuration
# BGP parameters mirror the CML test environment to ensure parity.
# Only the management IP (in hosts.yml) and credentials (in vault.yml) differ.
bgp:
  local_asn: 65002
  router_id: 2.2.2.2
  neighbors:
    - ip:           192.168.12.1
      remote_asn:   65001
      description:  "eBGP to R1"
      update_source: GigabitEthernet0/1
  networks:
    - 10.2.2.0/24
